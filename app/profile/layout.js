import "../globals.css";

export const metadata = {
  title: "LEAD CHRISTIAN DISCIPLESHIP",
  description: "Access your profile and manage your classes",
};

export default function ClassLayout({ children }) {
  return children;
}
