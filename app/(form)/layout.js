import "../globals.css";

export const metadata = {
  title: "LEAD CHRISTIAN DISCIPLESHIP",
  description: "Register for classes and manage your account",
};

export default function RegistrationLayout({ children }) {
  return children;
}
